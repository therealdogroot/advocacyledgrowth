/**
 * Persist a PDF download lead.
 * TODO: wire email provider (ESP / CRM) here.
 */
export async function saveLead(email: string): Promise<void> {
  // TODO: wire email provider — persist `email` to the chosen ESP/CRM.
  void email;
}
